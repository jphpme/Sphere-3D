# Dependencies
node_modules/
pnpm-lock.yaml
yarn.lock

# Build outputs
dist/
build/
*.tsbuildinfo
src-tauri/target/
# Generated platform scaffolding — created by `tauri android init`
# / `tauri ios init` from src-tauri/Cargo.toml + tauri.conf.json.
# CI regenerates these in .github/workflows/mobile.yml before the
# build runs; locally, `npm run dev:android` / `npm run dev:ios`
# does the same on first invocation. Untracking keeps the repo
# clean of platform-specific Gradle / Xcode artefacts that change
# on every Tauri version bump.
src-tauri/gen/
src/styles/tokens.css
# Generated by `npm run locales` from locales/*.json; see docs/I18N_PLAN.md
src/i18n/messages.ts
src/i18n/messages.*.ts

# Test / coverage artifacts
coverage/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
.env.local
.env.*.local

# Working files
_work/
# Personal Claude Code config is ignored, but specific *vendored*
# project files are tracked (the graphify skill + its SessionStart
# hook + shared project settings + shared subagents). `.claude/*`
# ignores direct children (e.g. settings.local.json — per-developer
# permissions), then the negations re-include the committed surface.
.claude/*
!.claude/settings.json
!.claude/agents/
!.claude/hooks/
!.claude/skills/

# Development
.cache/
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
.playwright-mcp/

# Local MCP client config — Claude Code reads `.mcp.json` at the
# repo root for project-scoped MCP server connections. Per-contributor;
# typically contains auth tokens (e.g. Penpot MCP). Never commit.
# A redacted `.mcp.json.example` may be committed as a template.
.mcp.json

# Runtime
.turbo/

# Wrangler local state — Miniflare D1/KV/R2/Queues volumes generated
# by `wrangler pages dev` and `wrangler d1 ... --local`. Per-contributor,
# never committed.
.wrangler/
.dev.vars
.dev.vars.local
# Vite reads `.env.local` at the repo root (envDir override in
# vite.config.ts). Per-developer values; never committed.
.env.local
.env.*.local
# Output of `npm run gen:node-key`; the wire-format public key is
# also persisted in node_identity.public_key, so this file is just
# a convenience for paste-into-portal flows.
node-public-key.txt

# `npm run setup` resumable state — per-operator Cloudflare resource
# IDs for this node. Holds no secret values (see scripts/lib/setup/
# state.ts), but the IDs are deployment-specific and never shared.
.terraviz-setup.json

# Test / coverage artifacts
coverage/
.terraviz/prod.env

# Graphify knowledge-graph output — generated by the `graphify`
# code-graph tool (graph.json / graph.html / GRAPH_REPORT.md + caches).
# Regenerable from source, multi-MB, per-developer. Never committed.
graphify-out/

# Weblate screenshot-capture output — PNGs + screenshots.json
# manifest produced by `npm run screenshots:capture`. Regenerated
# from the running app each run; uploaded to Weblate, never committed.
# See docs/WEBLATE_SCREENSHOT_SYNC_PLAN.md.
screenshots-out/

# Visual report output — PNGs + report.json + index.html produced by
# `npm run screenshots:report`. Regenerated each run; uploaded as a CI
# artifact / deployed, never committed. See docs/VISUAL_REPORT_PLAN.md.
report-out/

# Generated by scripts/luma-range-check (ramp frames + encoded probes)
scripts/luma-range-check/.frames/
scripts/luma-range-check/out/

# Generated by scripts/export-dataset-video (colour MP4 + SOS sidecars,
# plus a segment/basemap download cache). Regenerated on demand; the
# exported bundle is a deliverable, never committed.
export-out/
__pycache__/

# Local env (Vite envDir at repo root). Per-developer; never committed.
.env
.env.local
.env.*.local
